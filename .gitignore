# Rust / Cargo
/target
**/target/
**/*.rs.bk
# Cargo.lock is NOT ignored because we are building binaries that require strict dependency version locking for ESP32 hardware constraints.

# Keys and Secrets
/keys/
*.pem
*.bin

# Python Virtual Environments
/esp-env/
__pycache__/
*.pyc

# MacOS
.DS_Store
**/.DS_Store
proxy.log
supervisor-web/dist

# Local per-machine config (personal LAN IP)

# Exported macOS .app (double-clickable build artifact — keep out of the repo)
/CriticalInfra/
