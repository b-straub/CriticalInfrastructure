# Rust / Cargo
/target
**/target/
**/*.rs.bk
# Cargo.lock is NOT ignored because we are building binaries that require strict dependency version locking for ESP32 hardware constraints.

# Keys and Secrets
/keys/
*.pem
*.bin

# Python Virtual Environments
/esp-env/
__pycache__/
*.pyc

# ESP-IDF Bootloader Build Directory
/bootloader-build/build/
/bootloader-build/sdkconfig
/bootloader-build/dependencies.lock
/bootloader-build/managed_components/

# MacOS
.DS_Store
proxy.log
supervisor-web/dist

# Local per-machine config (personal LAN IP)
gui-client/ip_config.txt
